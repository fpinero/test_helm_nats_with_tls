apiVersion: v1
kind: Service
metadata:
  name: saas-platform-notificationservice
  labels:
    app: saas-platform-notificationservice
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 80
      name: http
    - port: 443
      targetPort: 443
      name: https
  selector:
    app: saas-platform-notificationservice
