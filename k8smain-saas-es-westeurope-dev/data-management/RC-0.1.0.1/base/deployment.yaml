apiVersion: apps/v1
kind: Deployment
metadata:
  name: data-management-api
  labels:
    app: data-management-api
spec:
  replicas: 1
  selector:
    matchLabels:
      app: data-management-api
  template:
    metadata:
      labels:
        app: data-management-api
      annotations:
        dapr.io/app-id: data-management-api
        dapr.io/enabled: 'true'
    spec:
      affinity:
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
            - weight: 10
              podAffinityTerm:
                labelSelector:
                  matchLabels:
                    app: data-management-api
                topologyKey: "kubernetes.io/hostname"
      containers:
          - name: data-management-api
            image: acrsaaseswesteuropedev.azurecr.io/data-management-api:0.1.0.0
            env:
              - name: ASPNETCORE_URLS
                value: "http://+:80"
              - name: ASPNETCORE_ENVIRONMENT
                value: "Development"  
            ports:
              - name: http
                containerPort: 80
            volumeMounts:
              - name: config
                mountPath: /app/Configuration
            resources: {}
      volumes:
      - name: config
        configMap:
          name: configurations-configmap
      restartPolicy: Always