apiVersion: v1
kind: Service
metadata:
  name: label-management-api
  labels:
    app: label-management-api
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 80
      name: http
  selector:
    app: label-management-api